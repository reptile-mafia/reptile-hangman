import React from 'react';
import {expect} from 'chai';
import {shallow, mount, render} from 'enzyme';
import GameBoard from '../../client/components/GameBoard';

describe('Guessed Letters', function() {

	it('Should show correct letters', ()=>{
		//let app = render(<GameBoard />);
		//expect(app.find('.guessedLetters').children.length.to.equal(0);
		expect(true).to.equal(true);
	})

	// it('Should reset to empty at game initialization', () => {
	// 	let app = render(<GameBoard />);
	// 	expect(app.find('.guessedLetters').children.length.to.equal(0);

	// })
});